# SmartCutElf 启动指南

## 📋 系统要求

- **Python**: 3.8+
- **FFmpeg**: 必须安装
- **内存**: 8GB+ 推荐
- **系统**: Windows / macOS / Linux

## 🚀 快速安装

### 1. 安装依赖

```bash
# 基础安装
pip install -r requirements.txt

# 使用国内镜像（如果慢）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 2. 安装 FFmpeg

详细步骤请查看 [FFmpeg安装指南](FFmpeg安装指南.md)

**快速验证：**
```bash
ffmpeg -version
```

### 3. 启动应用

```bash
python main.py
```

## ⚙️ 配置文件

编辑 `config.yaml` 自定义设置：

```yaml
# 剪辑设置
processing:
  target_duration_min: 180    # 最小时长（秒）
  target_duration_max: 300    # 最大时长（秒）
  max_workers: 4              # 并行数量
  transition_enabled: true    # 转场效果
  transition_type: fade       # 转场类型

# 高光检测
highlight:
  audio_weight: 0.4          # 音频权重
  video_weight: 0.4          # 视频权重

# AI 功能
subtitle:
  enabled: true              # 字幕生成
speech:
  recognition_model: base    # Whisper模型
  tts_enabled: true          # 语音合成

# 界面
ui:
  theme: dark                # 主题（dark/light）
```

## 🐛 故障排除

### FFmpeg 未找到
```bash
# 检查安装
ffmpeg -version

# 如未安装，查看 FFmpeg安装指南.md
```

### 依赖缺失
```bash
pip install -r requirements.txt
```

### Whisper 模型下载慢
- 首次运行自动下载（base 模型约 140MB）
- 改用更小的模型：在 config.yaml 中设置 `recognition_model: tiny`

### 内存不足
- 减少 `max_workers` 值（改为 2 或 1）
- 处理较小的视频文件

### PyQt5 安装失败
```bash
# Windows 用户
pip install PyQt5 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 📝 日志查看

日志位置：`logs/SmartCutElf_YYYYMMDD.log`

## 💡 使用建议

- 首次使用：用 1-2 分钟短视频测试
- 视频格式：MP4 效果最佳
- 输出目录：默认 `output/`

## 📚 更多文档

- [使用说明](使用说明.md) - 详细操作指南
- [FFmpeg安装](FFmpeg安装指南.md) - FFmpeg 配置
- [部署指南](部署指南.md) - 打包和分发
